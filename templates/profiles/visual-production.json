{
  "id": "visual-production",
  "label": "Vizuális produkció (könnyű)",
  "description": "Felület- és élménytervezés (UX/UI) + könnyű vizuális asset. A saját mappájába (és annak assets almappájába) ír, nem posztol kifelé. Alacsony egress. A képgeneráló/asset connector az ügynökönkénti .mcp.json-ban dől el. A DED osztja ezt a profilt.",
  "permissionMode": "strict",
  "filesystem": {
    "allow": [
      "Read(${AGENT_DIR}/**)",
      "Write(${AGENT_DIR}/**)",
      "Edit(${AGENT_DIR}/**)",
      "Read(${HOME}/Downloads/**)",
      "Bash(ls:*)",
      "Bash(cat:*)"
    ],
    "deny": [
      "Read(${HOME}/.ssh/**)",
      "Read(${HOME}/.aws/**)",
      "Read(${HOME}/.gnupg/**)",
      "Read(${HOME}/.env)",
      "Read(**/.env)",
      "Bash(sudo:*)",
      "Bash(rm:*)",
      "Bash(curl -X POST:*)"
    ]
  }
}
